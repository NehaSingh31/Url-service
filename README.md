Creating Url services simple using Core Java
there is main class UrlService and used internally command line argument
basically there are four method
first method>>> storeUrl method actually bind the uniqeID and url into a table 
second method>> getUniqeId method actually getting the specfic Url keys
third method>> getCount is returns latest count in this code.
fourth method getList method return json format using the pair of map.
